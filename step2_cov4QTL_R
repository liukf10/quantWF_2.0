#Version: 2.1
#created: Kefu liu(liukefu19@163.com)
#Maintainer: Kefu Liu
#Date: March 25, 2026
#Software: R
#V2.1: 添加outputdir参数支持指定输出目录
#260325 changed to Markdown report format, added PNG output for plots


#covar can be NULL
#quantfile must be RData or csv file. 
### package check ########
existpck <- rownames(installed.packages());
if(!"lme4" %in% existpck)
  stop("no lme4 package, please install the R pakcage before run me.")
#library(devtools)
#devtools::install_github("PMBio/peer")

### object check ##################
allobj <- ls();
if(!"quantfile" %in% allobj) {quantfile = NA;}
if(!"covarfile" %in% allobj) {covarfile = NA;}
if(!"genotype_pcfile" %in% allobj) {genotype_pcfile = NA}
if(!"IDfile" %in% allobj) {IDfile = NA}
if(!"output" %in% allobj) {output = NA;}
if(!"outputdir" %in% allobj) {outputdir = NA;}
if(!"funcdir" %in% allobj) {funcdir = NA;}
if(!"method" %in% allobj) {
  method = NA}
if(!"nFactor" %in% allobj) {nFactor = NA;}
if(!"nPCAchoose" %in% allobj) {
  nPCAchoose = NA;}
if(!"evaluate_method" %in% allobj) {
  evaluate_method = NA;}
if(!"BIC_cores" %in% allobj) {
  BIC_cores = NA;}
if(!"tools" %in% allobj) {
  tools = NA;}

opt = list(quantfile= quantfile,
           covarfile = covarfile,
           genotype_pcfile = genotype_pcfile,
           IDfile = IDfile,
           output = output, 
           outputdir = outputdir,
           funcdir = funcdir,
           method = method,
           nFactor = nFactor,
           nPCAchoose =nPCAchoose,
           evaluate_method =evaluate_method,
           BIC_cores =BIC_cores,
           tools = tools)
rmobj <- c(names(opt),"opt","option_list","allobj","allobj2","loginfo","existpck","funcpath")
allobj <- ls(); allobj <- allobj[!allobj %in% c("rmobj",rmobj)]

### 0.1 parameters evaluate ######
if(is.na(opt$outputdir)) {
  opt$outputdir = "."; 
  message("no outputdir, setting to current directory '.'")
}
if(is.na(opt$output)) {
  opt$output = "data";
  message("no output, setting to 'data' as prefix")
}
if(is.na(opt$funcdir)) {
  warning('funcdir is setting to NA, means preprocess_func.R in the default dir');
  if(file.exists("preprocess_func.R")) 
    source("preprocess_func.R") else 
      stop("No preprocess_func.R file in fundir path.")
  #V2.1: 使用outputdir参数创建plot和assets目录
  plot_dir <- get_plot_dir(opt$outputdir)
  assets_dir <- file.path(opt$outputdir, "assets")
  if(!dir.exists(assets_dir)) dir.create(assets_dir, recursive = TRUE)
} else {
  if(tail(unlist(strsplit(opt$funcdir,"")),1) == "/"){
    funcpath = paste0(opt$funcdir,"preprocess_func.R");
  } else {
    opt$funcdir <-  paste0(opt$funcdir,"/")
    funcpath = paste0(opt$funcdir,"preprocess_func.R");}
  if(file.exists(funcpath)) 
    source(funcpath) else
      stop("No preprocess_func.R file in fundir path.")
  #V2.1: 使用outputdir参数创建plot和assets目录
  plot_dir <- get_plot_dir(opt$outputdir)
  assets_dir <- file.path(opt$outputdir, "assets")
  if(!dir.exists(assets_dir)) dir.create(assets_dir, recursive = TRUE)
}
if(is.na(opt$method)) {
  opt$method = "pca"; message("Setting method as default: pca.")}
opt$method <- match.arg(opt$method,c("peer","pca"));
if(opt$method == "peer"){
  if(!"peer" %in% existpck)
    stop("no peer package, peer only install in linux system.")
}
if(is.na(opt$nPCAchoose)) {
  opt$nPCAchoose = "BE"; message("Setting evaluate_method as default: BE.")}
opt$nPCAchoose <- match.arg(opt$nPCAchoose,c("BE","Elbow"));
if(is.na(opt$evaluate_method)) {
  opt$evaluate_method = "pvca&pca"; message("Setting evaluate_method as default: pvca&pca.")}
if(is.na(opt$BIC_cores)) {
  opt$BIC_cores = 30; message("Setting BIC_cores as default: 30.")}
if(is.na(opt$tools)) {
  opt$tools = "tensorQTL"; message("tools for QTL analysis: tensorQTL or QTLtools.  QTLtools have Genebody and TSS, you can use GB/TSS/all to refer, Setting tools as default: tensorQTL.")}
opt$tools <- match.arg(opt$tools,c("tensorQTL","QTLtools","TSS.QTLtools","GB.QTLtools","all.QTLtools","both"));

### 0.2 read quant & covar&genotype_pc&IDinf info ####
#no quantfile should have quant obj
if(is.na(opt$quantfile)) {
  if("quant" %in% allobj) 
    message("using quant obj to do the process.") else
      stop ("no quantfile or quant object, see help info");
}
#check quantfile and load. RData or csv
if(!is.na(opt$quantfile)) {
  if(grepl("\\.RData$",opt$quantfile)) {
    if(file.exists(opt$quantfile)) 
      load(opt$quantfile) else 
        stop(paste0("No ",opt$quantfile," file."))
  } else if (file.exists(opt$quantfile))
    quant <- read.csv(opt$quantfile, row.names = 1) else 
      stop(paste0("No ",opt$quantfile," file."))
  if(!"quant" %in% ls()) 
    stop ("no quant object, see help info");
}
if(!is.numeric(as.matrix(quant))) 
  stop("quant are not all numeric.")
#check covarfile and load. (if offered) RData or csv
if(!is.na(opt$covarfile)) { #230201 updated
  if(grepl("\\.RData$",opt$covarfile)) {
    if(file.exists(opt$covarfile)) 
      load(opt$covarfile) else 
        stop(paste0("No ",opt$covarfile," file."))
  } else if (file.exists(opt$covarfile))
    covar <- read.table(opt$covarfile) else 
      stop(paste0("No ",opt$covarfile," file."))
}
#check genotype_pcfile and load. (if offered) RData or csv
if(!is.na(opt$genotype_pcfile)) { 
  if(grepl("\\.RData$",opt$genotype_pcfile)) {
    if(file.exists(opt$genotype_pcfile)) 
      load(opt$genotype_pcfile) else 
        stop(paste0("No ",opt$genotype_pcfile," file."))
  } else if (file.exists(opt$genotype_pcfile))
    genotype_pc <- read.table(opt$genotype_pcfile) else 
      stop(paste0("No ",opt$genotype_pcfile," file."))
}
if(!is.na(opt$IDfile)) { 
  if (file.exists(opt$IDfile)) {
    #gtf file only extract gene
    if(grepl("\\.RData$",opt$IDfile)) {
      load(opt$IDfile)
    } else if(grepl("gtf",opt$IDfile)) {
      if(!"rtracklayer" %in% existpck)
        stop("no rtracklayer package, gtf file read need rtracklayer.")
      suppressPackageStartupMessages(library(rtracklayer))
      IDinf <- import(opt$IDfile)
      IDinf <- as.data.frame(IDinf)
      IDinf <- subset(IDinf,type %in% "gene")
      IDinf$ID <- gsub("\\..*","",IDinf$gene_id)
      IDinf$ID[duplicated(IDinf$ID)] <- paste0(IDinf$ID[duplicated(IDinf$ID)],"_1")
      for( i in ncol(IDinf):1) {
        if(length(unique(IDinf[,i])) == 1) IDinf <- IDinf[,-i]
      }
    } else IDinf <- read.table(opt$IDfile) 
  } else stop(paste0("No ",opt$IDfile," file."))
}

otherobj <- ls(); 
otherobj <- otherobj[!otherobj %in% c("rmobj",rmobj)]
otherobj <- otherobj[!sapply(otherobj, function(x) is.function(get(x)))]
if(!"loginfo" %in% ls()) loginfo = list();

### 0.0 Initialize Markdown Report ########
# V2.1 revised: Initialize Markdown report after loading data
# Determine report file name (keep existing cov4QTL naming)
report_file <- file.path(opt$outputdir, paste0(opt$output, "_cov4QTL_report.md"))

# Check if input data has report_file object (from previous step)
loaded_report_file <- NULL
if("report_file" %in% otherobj) {
  if(exists("report_file")) {
    loaded_report_file <- report_file
  }
}

# Check if target report already exists
report_append <- file.exists(report_file)

# If no target report but have loaded report from different output, copy it
if(!report_append && !is.null(loaded_report_file) && file.exists(loaded_report_file)) {
  if(normalizePath(loaded_report_file, mustWork = FALSE) != normalizePath(report_file, mustWork = FALSE)) {
    file.copy(loaded_report_file, report_file, overwrite = TRUE)
    report_append <- TRUE
  }
}

# Initialize or append report
if(!report_append) {
  # Create new report
  report_lines <- c()
  report_lines <- c(report_lines, "# Step 2: QTL Covariate Preparation Report")
  report_lines <- c(report_lines, "")
  report_lines <- c(report_lines, paste0("**Date:** ", Sys.time()))
  report_lines <- c(report_lines, paste0("**Output:** ", opt$output))
  report_lines <- c(report_lines, paste0("**Output Directory:** ", opt$outputdir))
  report_lines <- c(report_lines, paste0("**Method:** ", opt$method))
  report_lines <- c(report_lines, paste0("**Tools:** ", opt$tools))
  report_lines <- c(report_lines, "")
  writeLines(report_lines, report_file)
} else {
  # Append to existing report
  cat("\n---\n\n", file = report_file, append = TRUE)
  cat("## Step 2: QTL Covariate Preparation Report (Additional Run)\n\n", file = report_file, append = TRUE)
  cat(paste0("**Date:** ", Sys.time(), "\n\n"), file = report_file, append = TRUE)
  cat(paste0("**Output:** ", opt$output, "\n\n"), file = report_file, append = TRUE)
  cat(paste0("**Output Directory:** ", opt$outputdir, "\n\n"), file = report_file, append = TRUE)
  cat(paste0("**Method:** ", opt$method, "\n\n"), file = report_file, append = TRUE)
  cat(paste0("**Tools:** ", opt$tools, "\n\n"), file = report_file, append = TRUE)
}

### 0.3 obj check ###########
if(!"covar" %in% ls()) stop ("no covar object, please check the data.");
if(!"genotype_pc" %in% ls()) stop ("no genotype_pc object, please check the data.");
if(!"IDinf" %in% ls()) stop ("no IDinf object, please check the data.");

#covar quant match
if("covar" %in% ls()) {
  if(!is.null(covar)) {
    #when covar is a vector will not match and transfer to data.frame 
    if(is.vector(covar)) {
      if(length(covar) == ncol(quant)) {
        covar <- data.frame(covar);
        colnames(covar) = "group" 
        rownames(covar) = colnames(quant);
        warning("covar is vector, please make sure the order is consistent with quant colnames.")
      } else stop("covar is vector and the number is not the same with quant column number.")
    } else if(length(dim(covar)) == 2) 
    { #quant and covar match one-by-one
      for (i in 1:(ncol(covar)+1)) {
        if(i == ncol(covar)+1) {
          if(all(colnames(quant) %in% rownames(covar)))
            break else i = 0;
        }
        if(all(colnames(quant) %in% covar[,i]))
          break
      }
      if(i == 0) stop("Not all sample match with covar.")
      if(i == (ncol(covar)+1)) {
        pos <- match(colnames(quant),rownames(covar))
        #230310 
        if(ncol(covar) == 1) {
          keepcolvar <- colnames(covar)
          covar <- data.frame(covar[pos,])
          colnames(covar) <- keepcolvar;
          rownames(covar) <- colnames(quant);
        } else covar <- covar[pos,];
      } else {
        pos <- match(colnames(quant),covar[,i]);
        covar <- covar[pos,]; covar <- covar[-i]; 
        rownames(covar) <- colnames(quant);
      } #covar & quant are matched 
    }
  }
}
#check and remove single value col in covar
if(!is.null(covar)) {
  remove_covar_col = NULL
  for( i in 1:ncol(covar)) {
    if(length(unique(covar[,i])) == 1) 
      remove_covar_col = c(remove_covar_col, i) 
  }
  if(!is.null(remove_covar_col)) {
    remove_covar_name = colnames(covar)[remove_covar_col];
    covar <- covar[,-remove_covar_col];
    message(paste0(paste0(remove_covar_name,collapse = ",")," have removed because only one element. \n"))
  }
}
#check if NA in covar
if(!is.null(covar)) {
  for( i in 1:ncol(covar))if(any(is.na(covar[,i]))) stop("NA not allowed in covar.") 
}
#genotype_pc covar match and merge 
colnames(genotype_pc) <- paste0("genopc", 1:ncol(genotype_pc))
pos <- match(rownames(covar),rownames(genotype_pc))
if(any(is.na(pos))) stop("Not all sample in covar match with genotype_pc file.")
genotype_pc <- genotype_pc[pos,]
covar<- cbind(covar,genotype_pc)
#IDinf check
#match IDinf and quant
if(!"ID" %in% colnames(IDinf)) 
  stop("IDinf should have ID col.") else {
    pos <- match(rownames(quant),IDinf$ID)
    if(any(is.na(pos))) stop("Not all ID in IDinf match with quantfile.")
    IDinf <- IDinf[pos,]
  }

report_lines <- c(report_lines, "## Output Files")
report_lines <- c(report_lines, "")
report_lines <- c(report_lines, "### Covariate Files")
report_lines <- c(report_lines, "")
report_lines <- c(report_lines, paste0("- `", opt$output,"_",opt$method,"_covariate4QTL.txt`"))
report_lines <- c(report_lines, "")

report_lines <- c(report_lines, "### Expression Matrix Files")
report_lines <- c(report_lines, "")

if(grepl("tensorQTL|both",opt$tools)) {
  report_lines <- c(report_lines, paste0("- `", opt$output,".tss.tensor.bed` (for tensorQTL)"))
}
if(grepl("QTLtools|both",opt$tools)) {
  if(grepl("tss|all|both",opt$tools)|opt$tools == "QTLtools") {
    report_lines <- c(report_lines, paste0("- `", opt$output,".tss.bed` (for QTLtools)"))
  }
  if(grepl("GB|all|both",opt$tools)) {
    report_lines <- c(report_lines, paste0("- `", opt$output,".genebody.bed` (for QTLtools)"))
  }
}
report_lines <- c(report_lines, "")

report_lines <- c(report_lines, "## Input Information")
report_lines <- c(report_lines, "")
report_lines <- c(report_lines, paste0("- **Quantification file:** ", ifelse(is.na(opt$quantfile), "Using quant object", opt$quantfile)))
report_lines <- c(report_lines, paste0("- **Covariate file:** ", ifelse(is.na(opt$covarfile), "Not provided", opt$covarfile)))
report_lines <- c(report_lines, paste0("- **Genotype PC file:** ", ifelse(is.na(opt$genotype_pcfile), "Not provided", opt$genotype_pcfile)))
report_lines <- c(report_lines, paste0("- **ID file:** ", ifelse(is.na(opt$IDfile), "Not provided", opt$IDfile)))
report_lines <- c(report_lines, paste0("- **Number of features:** ", nrow(quant)))
report_lines <- c(report_lines, paste0("- **Number of samples:** ", ncol(quant)))
report_lines <- c(report_lines, "")

### 1. hidden covariate search #########
cat('-----data preprocess 2 hidden covariate search----- \n')

report_lines <- c(report_lines, "## Hidden Covariate Search")
report_lines <- c(report_lines, "")

if(opt$method == "peer") {
  if(is.na(opt$nFactor))  {
    if(ncol(quant) < 150) 
      nFactor = 15 else if (ncol(quant) < 250)
        nFactor = 30 else if (ncol(quant) < 350)
          nFactor = 45 else nFactor = 60
          message("Setting nFactor depend on sample size (GTEx suggestion): ", nFactor)
  } else nFactor = opt$nFactor
  if(nFactor < 1) stop("wrong nFactor.")
  suppressPackageStartupMessages(library(peer))
  expr = t(as.matrix(quant)) 
  model = PEER()  # create the model object
  invisible(PEER_setPhenoMean(model,expr))# set the observed data
  invisible(PEER_setNk(model,nFactor)) # gradient number of factors
  invisible(PEER_getNk(model))
  invisible(PEER_setAdd_mean(model, TRUE))  # include an additional factor (covariate) to account for the mean expression
  #covar1 = NULL 1.obs+hid and no keep; 2.covar is NULL
  if(!is.null(covar)) {
    invisible(PEER_setCovariates(model, as.matrix(covar)))  # adding covariates has no effect on the model?
  }
  #PEER_setNmax_iterations(model, 100)  # If the model is not converged after the default 1,000 iterations, and the variance of the residuals keeps decreasing, choose a higher value of iterations, e.g., 10,000.
  time = system.time(PEER_update(model))  # perform the inference
  
  factors = PEER_getX(model)  # inferred confounders samples x PEER factors
  factors=factors[,-1];
  rownames(factors) = rownames(expr);
  colnames(factors) = paste0("peer",1:ncol(factors))
  weights = PEER_getW(model)  # their weights
  precision = PEER_getAlpha(model)     # precision (inverse variance) of the weights
  residuals = PEER_getResiduals(model) # the residual dataset
  #plot(precision)
  #PEER_plotModel(model)
  #Variance_factor_plot
  {
    Variance = apply(factors,2,var);
    Variance<-sort(Variance, decreasing=TRUE); 
    Variance<-100*Variance/sum(Variance)
    
    # Save PEER variance factor to both plot/ (PDF) and assets/ (PNG)
    peer_pdf <- file.path(plot_dir, paste0("peer_variance_factor_",opt$output,".pdf"))
    peer_png <- file.path(assets_dir, paste0("peer_variance_factor_",opt$output,".png"))
    
    pdf(peer_pdf,width=5,height=5)
    plot(Variance,type="o",pch=19,col="red",xlab="Factors")
    dev.off()
    
    png(peer_png,width=500,height=500,res=150)
    plot(Variance,type="o",pch=19,col="red",xlab="Factors")
    dev.off()
  }
  if(is.null(covar))
    covar_hid <- data.frame(factors) else
      covar_hid <- data.frame(covar,factors)
  
  report_lines <- c(report_lines, paste0("- **Method:** PEER (Probabilistic Estimation of Expression Residuals)"))
  report_lines <- c(report_lines, paste0("- **Number of factors:** ", nFactor))
  report_lines <- c(report_lines, paste0("- **Runtime:** ", round(time[3], 2), " seconds"))
  report_lines <- c(report_lines, "")
  report_lines <- c(report_lines, "### PEER Factor Variance Plot")
  report_lines <- c(report_lines, "")
  report_lines <- c(report_lines, paste0("![PEER Variance](assets/peer_variance_factor_", opt$output, ".png)"))
  report_lines <- c(report_lines, "")
  report_lines <- c(report_lines, paste0("- **PDF version:** `plot/peer_variance_factor_", opt$output, ".pdf`"))
  report_lines <- c(report_lines, "")
}
if(opt$method == "pca"){
  expr = t(as.matrix(quant)) 
  prcompResult<-prcomp(expr,center=TRUE,scale.=TRUE)
  #This should take less than a minute.
  PCs<-prcompResult$x 
  dim(PCs)
  importanceTable<-summary(prcompResult)$importance
  PVEs<-importanceTable[2,]
  #sum(PVEs) #Theoretically, this should be 1.
  #pdf("PVEs.pdf")
  #plot(PVEs,xlab="PC index",ylab="PVE")
  resultRunElbow<-runElbow(prcompResult=prcompResult)
  #print(resultRunElbow)
  K_elbow<-resultRunElbow
  resultRunBE<-runBE(expr,B=25,alpha=0.05,mc.cores=1)
  #print(resultRunBE$numOfPCsChosen)
  K_BE<-resultRunBE$numOfPCsChosen 
  
  # Save PCA variance factor (scree plot) to both plot/ (PDF) and assets/ (PNG)
  pca_var_pdf <- file.path(plot_dir, paste0("PCA_variance_factor_",opt$output,".pdf"))
  pca_var_png <- file.path(assets_dir, paste0("PCA_variance_factor_",opt$output,".png"))
  
  pdf(pca_var_pdf,width=5,height=5)
  print(makeScreePlot(prcompResult,labels=c("Elbow","BE"),values=c(K_elbow,K_BE),
                titleText=opt$output))
  dev.off()
  
  png(pca_var_png,width=500,height=500,res=150)
  print(makeScreePlot(prcompResult,labels=c("Elbow","BE"),values=c(K_elbow,K_BE),
                titleText=opt$output))
  dev.off()
  
  if(opt$nPCAchoose=="BE") PCsTop<-PCs[,1:K_BE] #default is choose K_BE
  if(opt$nPCAchoose=="Elbow") PCsTop<-PCs[,1:K_elbow]
  if(!is.null(covar)){
    if(ncol(covar) != 1) {
      covar2 <- covar;
      for (i in 1:ncol(covar2)) {
        if(!class(covar2[,i]) %in% c("numeric","integer"))
          covar2[,i] <- as.numeric(as.factor(covar2[,i]))
      }
    }
    if(ncol(covar) == 1) {
      covar2 <- covar;
      if(!inherits(covar[,1],c("numeric","integer")))
        covar2[,1] <- as.numeric(as.factor(covar2[,1]))
    }
    knowncovFiltered<-filterKnownCovariates(covar2,PCsTop,unadjustedR2_cutoff=0.9)
    PCsTop<-scale(PCsTop)
    covar_hid <-data.frame(covar[,colnames(covar) %in% colnames(knowncovFiltered)],PCsTop)
  } else covar_hid <- scale(PCsTop)
  
  report_lines <- c(report_lines, paste0("- **Method:** PCA-based hidden covariate extraction"))
  report_lines <- c(report_lines, paste0("- **PC selection method:** ", opt$nPCAchoose))
  report_lines <- c(report_lines, paste0("- **Number of PCs selected:** ", ncol(PCsTop)))
  report_lines <- c(report_lines, paste0("  - Elbow method suggested: ", K_elbow, " PCs"))
  report_lines <- c(report_lines, paste0("  - BE method suggested: ", K_BE, " PCs"))
  report_lines <- c(report_lines, "")
  report_lines <- c(report_lines, "### PCA Scree Plot")
  report_lines <- c(report_lines, "")
  report_lines <- c(report_lines, paste0("![PCA Scree](assets/PCA_variance_factor_", opt$output, ".png)"))
  report_lines <- c(report_lines, "")
  report_lines <- c(report_lines, paste0("- **PDF version:** `plot/PCA_variance_factor_", opt$output, ".pdf`"))
  report_lines <- c(report_lines, "")
} 

write.table(t(covar_hid),file=file.path(opt$outputdir, paste0(opt$output,"_",opt$method,"_covariate4QTL.txt")),col.names=NA,quote=F)
record <- paste0(opt$output,"_",opt$method,"_covariate4QTL.txt was saved.")
message(record)
record <- paste0(record,"\n")

report_lines <- c(report_lines, "### Output Covariate File")
report_lines <- c(report_lines, "")
report_lines <- c(report_lines, paste0("- **Covariate file:** `", opt$output,"_",opt$method,"_covariate4QTL.txt`"))
report_lines <- c(report_lines, paste0("- **Number of covariates:** ", ncol(covar_hid)))
report_lines <- c(report_lines, paste0("- **Number of samples:** ", nrow(covar_hid)))
report_lines <- c(report_lines, "")

### 2 merge info ############

IDinf$seqnames <- gsub("chr","",IDinf$seqnames)
pos <- IDinf$seqnames %in% 1:22
quant <- quant[pos,]
IDinf <- IDinf[pos,]
IDinf$TSS <- IDinf$start
IDinf$TSS[IDinf$strand == "-"] <- IDinf$end[IDinf$strand == "-"]
IDinf$TSSstart = IDinf$TSS -1;
IDinf$TSSend = IDinf$TSS;
IDinf <- IDinf[,c("seqnames","TSSstart","TSSend","gene_id","strand","start","end")]
IDinf$seqnames <- as.numeric(IDinf$seqnames)
colnames(IDinf)[1] <- "#chr"

###For tensorQTL 
if(grepl("tensorQTL|both",opt$tools)) {
  tssbed <- cbind(IDinf[,1:4],quant)
  colnames(tssbed)[2:3] <- c("start","end")
  tssbed <- tssbed[order(tssbed$start),]
  tssbed <- tssbed[order(tssbed$`#chr`),]
  
  # Write TSS-based BED file
  write.table(
    tssbed,
    file = file.path(opt$outputdir, paste0(opt$output,".tss.tensor.bed")),
    quote = FALSE, sep = "\t",row.names = FALSE, col.names = TRUE)
  
  record <- paste0(record, opt$output,".tss.tensor.bed was saved for tensorQTL\n")
  message("Done! The file 'tss.tensor.bed' for tensorQTL have been generated successfully.")
  
  report_lines <- c(report_lines, "## TensorQTL Format Output")
  report_lines <- c(report_lines, "")
  report_lines <- c(report_lines, paste0("- **TSS BED file:** `", opt$output,".tss.tensor.bed`"))
  report_lines <- c(report_lines, paste0("  - Number of features: ", nrow(tssbed)))
  report_lines <- c(report_lines, paste0("  - Number of samples: ", ncol(tssbed) - 4))
  report_lines <- c(report_lines, "")
}
###  For QTltools 
if(grepl("QTLtools|both",opt$tools)) {
  report_lines <- c(report_lines, "## QTLtools Format Output")
  report_lines <- c(report_lines, "")
  
  if(grepl("tss|all|both",opt$tools)|opt$tools == "QTLtools") {
    message("Creating TSS-based BED file...")
    tssbed <- cbind(IDinf[,c(1,2,3,4,4,5)],quant)
    colnames(tssbed)[2:5] <- c("start","end","PID","GID") 
    tssbed <- tssbed[order(tssbed$start),]
    tssbed <- tssbed[order(tssbed$`#chr`),]
    
    # Write TSS-based BED file
    write.table(
      tssbed,
      file = file.path(opt$outputdir, paste0(opt$output,".tss.bed")),
      quote = FALSE,sep = "\t",row.names = FALSE,col.names = TRUE)
    record <- paste0(record, opt$output,".tss.bed was saved for QTLtools\n")
    message("Done! The file 'expression.tss.bed' for QTLtools have been generated successfully.")
    
    report_lines <- c(report_lines, paste0("- **TSS BED file:** `", opt$output,".tss.bed`"))
    report_lines <- c(report_lines, paste0("  - Number of features: ", nrow(tssbed)))
    report_lines <- c(report_lines, paste0("  - Number of samples: ", ncol(tssbed) - 6))
    report_lines <- c(report_lines, "")
  }
  if(grepl("GB|all|both",opt$tools)) {
    message("Creating Genebody-based BED file...")
    genebody_bed <- cbind(IDinf[,c(1,6,7,4,4,5)],quant)
    colnames(genebody_bed)[4:5] <- c("PID","GID") 
    #Write Genebody-based BED 
    write.table(
      genebody_bed,
      file = file.path(opt$outputdir, paste0(opt$output,".genebody.bed")),
      quote = FALSE,sep = "\t", row.names = FALSE,col.names = TRUE)
    record <- paste0(record, opt$output,".genebody.bed was saved for QTLtools\n")
    message("Done! The file 'expression.genebody.bed' for QTLtools have been generated successfully.")
    
    report_lines <- c(report_lines, paste0("- **Genebody BED file:** `", opt$output,".genebody.bed`"))
    report_lines <- c(report_lines, paste0("  - Number of features: ", nrow(genebody_bed)))
    report_lines <- c(report_lines, paste0("  - Number of samples: ", ncol(genebody_bed) - 6))
    report_lines <- c(report_lines, "")
  }
}

###  covariants correlation #######

if(!is.null(covar_hid)){
  if(ncol(covar_hid) != 1) {
    covar_hid2 <- covar_hid;
    for (i in 1:ncol(covar_hid2)) {
      if(!class(covar_hid2[,i]) %in% c("numeric","integer"))
        covar_hid2[,i] <- as.numeric(as.factor(covar_hid2[,i]))
    }
  }
}
if(!is.null(covar_hid)) {
  cat("- Draw covar correlation plot - \n")
  #plot 
  suppressPackageStartupMessages(require(corrplot))
  col2 <- colorRampPalette(c("#053061","#2166AC","#4393C3","#92C5DE",
                             "#D1E5F0", "#FFFFFF","#FDDBC7","#F4A582",
                             "#D6604D","#B2182B","#67001F"))
  
  # Save covariate correlation plot to both plot/ (PDF) and assets/ (PNG)
  cor_pdf <- file.path(plot_dir, paste0(opt$output,"_",opt$method,"_covhid_cor.pdf"))
  cor_png <- file.path(assets_dir, paste0(opt$output,"_",opt$method,"_covhid_cor.png"))
  
  pdf(cor_pdf, width = 10, height = 10)
  if(!is.null(covar_hid))
    if(ncol(covar_hid) != 1)
      corrplot(cor(covar_hid2),p.mat = cor.mtest(covar_hid2)$p, col = col2(200),
               insig = "p-value", order = "original",method = "ellipse",
               type = "lower", tl.pos = "d")
  dev.off()
  
  png(cor_png, width = 1000, height = 1000, res = 150)
  if(!is.null(covar_hid))
    if(ncol(covar_hid) != 1)
      corrplot(cor(covar_hid2),p.mat = cor.mtest(covar_hid2)$p, col = col2(200),
               insig = "p-value", order = "original",method = "ellipse",
               type = "lower", tl.pos = "d")
  dev.off()
  
  report_lines <- c(report_lines, "## Covariate Correlation Analysis")
  report_lines <- c(report_lines, "")
  report_lines <- c(report_lines, "Correlation analysis was performed to assess relationships between covariates.")
  report_lines <- c(report_lines, "")
  report_lines <- c(report_lines, paste0("![Covariate Correlation](assets/", opt$output,"_",opt$method,"_covhid_cor.png)"))
  report_lines <- c(report_lines, "")
  report_lines <- c(report_lines, paste0("- **PDF version:** `plot/", opt$output,"_",opt$method,"_covhid_cor.pdf`"))
  report_lines <- c(report_lines, "")
}

###  pvca  ########
if(grepl("pvca|PVCA",opt$evaluate_method)) {
  # V2.1: Check data validity before PVCA
  if(!exists("quant") || is.null(quant) || nrow(quant) == 0 || ncol(quant) == 0) {
    message("quant data is empty or NULL, skipping PVCA plot.")
  } else if(!exists("covar_hid2") || is.null(covar_hid2)) {
    message("covar_hid2 is not available, skipping PVCA plot.")
  } else {
  if("lme4" %in% existpck) { #pvca
    if(!is.null(covar)) {
      cat("- Draw pvca plot - \n")
      if(ncol(covar) > 0) {
        # Define plotpdfname for consistent naming
        plotpdfname = paste0("_",opt$method,"_cov4QTL")
        
        pvca_pdf <- file.path(plot_dir, paste0(opt$output,plotpdfname,"_pvca.pdf"))
        pvca_png <- file.path(assets_dir, paste0(opt$output,plotpdfname,"_pvca.png"))
        
        pdf(pvca_pdf,width = 12,height = 6)
        suppressMessages(pvcaresult <- pvca_evaluate(quant,covar_hid2, pct_threshold = 0.6,output = "cov4QTL"))
        print(pvcaresult$pic)
        dev.off()
        
        png(pvca_png,width = 1200,height = 600,res = 150)
        suppressMessages(pvcaresult <- pvca_evaluate(quant,covar_hid2, pct_threshold = 0.6,output = "cov4QTL"))
        print(pvcaresult$pic)
        dev.off()
        
        message("pvca plot was finished.")
        
        report_lines <- c(report_lines, "## PVCA Evaluation")
        report_lines <- c(report_lines, "")
        report_lines <- c(report_lines, "Principal Variance Component Analysis (PVCA) was performed to evaluate the contribution of covariates to total variance.")
        report_lines <- c(report_lines, "")
        report_lines <- c(report_lines, paste0("![PVCA Plot](assets/", opt$output,plotpdfname,"_pvca.png)"))
        report_lines <- c(report_lines, "")
        report_lines <- c(report_lines, paste0("- **PDF version:** `plot/", opt$output,plotpdfname,"_pvca.pdf`"))
        report_lines <- c(report_lines, paste0("- **Threshold:** 0.6"))
        report_lines <- c(report_lines, "")
      }  # end if(ncol(covar) > 0)
    }  # end if(!is.null(covar))
  } else  cat(" - no lme4 pcakge, not draw PVCA plot.- \n")
  }  # end data validity check
}

### BIC #######
if(grepl("bic|BIC",opt$evaluate_method)) {
  # V2.1: Check data validity before BIC
  valid_bicdata <- TRUE
  if(!exists("quant") || is.null(quant) || nrow(quant) == 0 || ncol(quant) == 0) {
    message("quant is not available or empty, skipping BIC plot.")
    valid_bicdata <- FALSE
  } else if(!exists("covar_hid2") || is.null(covar_hid2) || nrow(covar_hid2) == 0) {
    message("covar_hid2 is empty, skipping BIC plot.")
    valid_bicdata <- FALSE
  }
  
  if(valid_bicdata) {
  cat("- Draw BIC plot - \n")
  cat("Note: BIC evaluation may take a long time depending on the number of features and cores used.\n")
  
  # Define plotpdfname for consistent naming
  plotpdfname = paste0("_",opt$method,"_cov4QTL")
  
  bic_pdf <- file.path(plot_dir, paste0(opt$output,plotpdfname,"_bic.pdf"))
  bic_png <- file.path(assets_dir, paste0(opt$output,plotpdfname,"_bic.png"))
  
  pdf(bic_pdf,width = 12,height = 6)
  BICresult <- BIC_evaluate(quant,covar_hid2,num_cores = opt$BIC_cores,output = paste0("BIC estimation"))
  print(BICresult$pic)
  dev.off()
  
  png(bic_png,width = 1200,height = 600,res = 150)
  BICresult <- BIC_evaluate(quant,covar_hid2,num_cores = opt$BIC_cores,output = paste0("BIC estimation"))
  print(BICresult$pic)
  dev.off()
  
  message("BIC plot was finished.")
  
  report_lines <- c(report_lines, "## BIC Evaluation")
  report_lines <- c(report_lines, "")
  report_lines <- c(report_lines, "Bayesian Information Criterion (BIC) evaluation was performed to assess model fit.")
  report_lines <- c(report_lines, "")
  report_lines <- c(report_lines, paste0("![BIC Plot](assets/", opt$output,plotpdfname,"_bic.png)"))
  report_lines <- c(report_lines, "")
  report_lines <- c(report_lines, paste0("- **PDF version:** `plot/", opt$output,plotpdfname,"_bic.pdf`"))
  report_lines <- c(report_lines, paste0("- **Cores used:** ", opt$BIC_cores))
  report_lines <- c(report_lines, "")
  }  # end if(valid_bicdata)
}
cat(record)
cat("----step2 cov4QTL runing finished ------\n")

### Report Summary ########
report_lines <- c(report_lines, "## Plot Files")
report_lines <- c(report_lines, "")
report_lines <- c(report_lines, "### PDF Files (in plot/ directory)")
report_lines <- c(report_lines, "")
report_lines <- c(report_lines, paste0("- `", opt$output,"_",opt$method,"_covhid_cor.pdf` (covariate correlation)"))
if(grepl("pvca|PVCA",opt$evaluate_method)) {
  report_lines <- c(report_lines, paste0("- `", opt$output,"_",opt$method,"_cov4QTL_pvca.pdf` (PVCA plot)"))
}
if(grepl("bic|BIC",opt$evaluate_method)) {
  report_lines <- c(report_lines, paste0("- `", opt$output,"_",opt$method,"_cov4QTL_bic.pdf` (BIC plot)"))
}
if(opt$method == "peer") {
  report_lines <- c(report_lines, paste0("- `peer_variance_factor_",opt$output,".pdf` (PEER factor variance)"))
}
if(opt$method == "pca") {
  report_lines <- c(report_lines, paste0("- `PCA_variance_factor_",opt$output,".pdf` (PCA scree plot)"))
}
report_lines <- c(report_lines, "")

report_lines <- c(report_lines, "### PNG Files (in assets/ directory)")
report_lines <- c(report_lines, "")
report_lines <- c(report_lines, paste0("- `", opt$output,"_",opt$method,"_covhid_cor.png` (covariate correlation)"))
if(grepl("pvca|PVCA",opt$evaluate_method)) {
  report_lines <- c(report_lines, paste0("- `", opt$output,"_",opt$method,"_cov4QTL_pvca.png` (PVCA plot)"))
}
if(grepl("bic|BIC",opt$evaluate_method)) {
  report_lines <- c(report_lines, paste0("- `", opt$output,"_",opt$method,"_cov4QTL_bic.png` (BIC plot)"))
}
if(opt$method == "peer") {
  report_lines <- c(report_lines, paste0("- `peer_variance_factor_",opt$output,".png` (PEER factor variance)"))
}
if(opt$method == "pca") {
  report_lines <- c(report_lines, paste0("- `PCA_variance_factor_",opt$output,".png` (PCA scree plot)"))
}
report_lines <- c(report_lines, "")

report_lines <- c(report_lines, "### Analysis Summary")
report_lines <- c(report_lines, "")
if(exists("nFactor")) {
  report_lines <- c(report_lines, paste0("- **Hidden factors (nFactor):** ", nFactor))
}
if(exists("K_BE")) {
  report_lines <- c(report_lines, paste0("- **PCs selected by BE method:** ", K_BE))
}
if(exists("K_elbow")) {
  report_lines <- c(report_lines, paste0("- **PCs selected by Elbow method:** ", K_elbow))
}
report_lines <- c(report_lines, "")
report_lines <- c(report_lines, "---")
report_lines <- c(report_lines, "")
report_lines <- c(report_lines, paste0("*Report generated at:* ", Sys.time()))
report_lines <- c(report_lines, "")
report_lines <- c(report_lines, "*Generated by step2_cov4QTL v2.1*")

# Write report file
writeLines(report_lines, report_file)
message("\nMarkdown report saved to: ", report_file)
